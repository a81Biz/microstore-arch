export enum OrderStatus {
  PENDING = 'pending',
  PAID = 'paid',
  IN_PRODUCTION = 'in_production',
  PACKAGED = 'packaged',
  SHIPPED = 'shipped',
  IN_TRANSIT = 'in_transit',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
  REFUNDED = 'refunded'
}
