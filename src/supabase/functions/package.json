{
  "name": "@micro-store/edge-functions-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Test suite for Supabase Edge Functions (Node/vitest mock-based)",
  "scripts": {
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "vitest": "^3.2.6"
  }
}
